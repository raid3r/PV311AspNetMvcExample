@model Profession
@{
    ViewData["Title"] = "Edit profession";
}
<div>
    <h1 class="display-4">@ViewData["Title"]</h1>

    <form method="post" enctype="multipart/form-data" asp-controller="Profession" asp-action="Edit">
        <div class="card">
            <div class="card-body">
                <div class="mb-3">
                    @Html.LabelFor(x => x.Title, new { @class = "form-label" })
                    @Html.TextBoxFor(x => x.Title, new { @class = "form-control" })
                    @Html.ValidationMessageFor(x => x.Title, null, new { @class = "text-danger" })
                </div>
            </div>
            <div class="card-footer">
                <button class="btn btn-primary" type="submit">Save</button>
            </div>
        </div>
    </form>

    <a asp-area=""
       asp-controller="Profession"
       asp-action="Index"
       class="btn btn-outline-success">Return</a>
</div>